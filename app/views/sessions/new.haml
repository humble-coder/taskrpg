%h1 Sign in
%div{"data-role" => "content"}
  = form_for(:session, url: sessions_path) do |f|
    .field
      = f.label :email
      = f.text_field :email
    .field
      = f.label :password
      = f.password_field :password
    .actions
      = f.submit "Sign in"
  %p
    New to Task RPG? #{link_to "Sign up today!", signup_path}
    %br
    Forgot your password? #{link_to "Reset it!", new_password_reset_path}